name: hf-keepalive

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      
      - name: Send daily prompt to HF Space
        env:
          HF_SPACE_URL: https://yousefsg-chatgpt-api.hf.space
          API_KEY: gptkey0
        run: python3 hf_keepalive.py
